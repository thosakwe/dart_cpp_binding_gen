/// Specifies that a C++ interop delegate should be generated for this class.
const Binding binding = const Binding._();

/// Specifies that a library should generate a Dart native extension entry point.
const Extension extension = const Extension._();

/// Specifies that a C++ interop delegate should be generated for this class.
class Binding {
  const Binding._();
}

/// Specifies that a library should generate a Dart native extension entry point.
class Extension {
  const Extension._();
}
